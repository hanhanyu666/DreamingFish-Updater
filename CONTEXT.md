# 梦鱼整合包更新系统

本上下文描述为 Minecraft 服务器整合包制作、发布和应用更新时使用的领域概念。

## 统一语言

**玩家端更新器**：
玩家电脑上负责更新本地整合包，并展示更新信息与日志的应用程序。它不负责 Minecraft 账号认证、实例管理、游戏安装或游戏启动。
_避免使用_：启动器、客户端

**桌面封面界面**：
玩家端更新器在电脑上显示的横向封面式主窗口，以服务器实景为全窗口背景，左侧承载身份信息，右下承载固定进度区。它不是网页移动端布局，也不会按手机页面重新堆叠主内容。
_避免使用_：手机端界面、移动端分页、竖屏布局

**管理端**：
服主用于制作和发布整合包更新的应用程序。
_避免使用_：服务端、后台

**Minecraft 启动器**：
负责游戏配置与启动的现有第三方程序，例如 PCL2、HMCL 或 Prism Launcher。
_避免使用_：玩家端更新器

**启动检查**：
Minecraft 启动器在游戏启动前触发的一次玩家端更新器运行。它始终显示窗口并展示服务器信息，并按在线验证或网络故障策略决定是否给出启动许可。
_避免使用_：启动游戏

**启动许可**：
玩家端更新器允许本次游戏启动的结果。在线更新成功和已验证离线许可表示本地状态可信；“未验证离线启动”是更新服务器不可达且没有签名基线时的明确可用性例外。Minecraft 启动器获得许可后可以立即继续，玩家端更新器窗口是否仍然显示与该许可无关。
_避免使用_：关闭更新器、完成启动

**已验证安装**：
完整应用过一个可信发布版本，并通过完整性校验的本地整合包状态。已验证不代表它一定是当前最新版本。
_避免使用_：最新版本、可用文件夹

**离线启动许可**：
更新服务不可用时，玩家端更新器根据最近一次已验证安装给出的启动许可。它不能用于安装未完成、文件损坏或发布签名无效的状态。
_避免使用_：跳过校验、强制启动

**未验证离线启动**：
更新服务不可用且实例没有签名基线时，玩家端明确跳过本次整合包检查后给出的启动许可。它必须在界面和日志中标记为未验证，并且不能由签名、清单、路径、事务、哈希或已验证安装完整性错误触发。
_避免使用_：离线许可、验证通过、安全安装

**非托管模组**：
存在于玩家本地 `mods` 目录，但不属于当前发布版本的模组。玩家端更新器会提示它的存在，但默认保留该文件并允许继续。
_避免使用_：多余模组、非法模组

**黑名单模组**：
管理端明确标记为不允许连接目标服务器时使用的非托管模组。它不影响玩家启动 Minecraft 或进行单人游戏，也不构成反作弊安全边界。
_避免使用_：未知模组、病毒

**服务器准入**：
目标服务器根据客户端状态决定是否允许玩家加入的结果。服务器准入与 Minecraft 启动及单人游戏相互独立。
_避免使用_：启动许可、终止游戏

**基础更新能力**：
不依赖特定 Minecraft 版本或模组加载器的文件发布、下载、校验与安装能力。
_避免使用_：全版本完整支持

**官方支持矩阵**：
经过项目正式测试，并支持对应高级能力的 Minecraft 版本与模组加载器组合。矩阵之外的平台仍可使用基础更新能力。
_避免使用_：支持所有版本

**自托管管理端**：
由服主下载、部署并自行运营的管理端实例，其配置、发布数据、密钥和存储均由该服主控制。
_避免使用_：中央平台、官方云服务

**整合包项目**：
由同一套管理端独立管理和发布的一套整合包产品，拥有自己的身份、发布历史、玩家端配置和文件源。
_避免使用_：服务器、管理端实例

**标准整合包目录**：
服主为一个整合包项目专门维护的期望内容来源。它只需包含服主希望发布的目录和文件，不必是完整游戏目录，并且独立于玩家实例和运行中的 Minecraft 服务端目录。
_避免使用_：工作区、玩家实例、服务端目录

**发布预览**：
管理端扫描标准整合包目录后生成、等待服主确认的拟发布变更集合。它尚未签名或公开，不属于发布版本。
_避免使用_：草稿版本、自动发布

**发布版本**：
整合包项目在某个时点不可变的完整目标状态，按项目的单一发布线依次排列。它不是依赖前一版本才能执行的增量补丁。
_避免使用_：补丁包、更新步骤

**分发基线**：
正式整合包下载包所对应的可信发布版本。玩家端可据此识别旧包原本包含的托管文件，并从任意旧基线直接收敛到最新发布版本。
_避免使用_：初始版本、增量起点、安装记录

**目标文件清单**：
一个发布版本声明应由系统管理的全部文件及其内容身份。客户端通过它比较本地状态，只获取实际缺少或不同的内容。
_避免使用_：变更列表、全量下载包

**强制托管文件**：
本地内容必须与当前发布版本一致的文件。玩家的本地修改可以被更新覆盖，发布版本移除该文件时也可以从本地移除。
_避免使用_：只读文件、锁定文件

**放弃管理**：
服主从目标文件清单移除一个既有托管文件时，明确把该文件的后续所有权交还玩家；玩家本地副本会被保留，后续发布也不再检查、覆盖或删除它。文件重新出现在标准整合包目录后才重新成为托管文件。
_避免使用_：忽略删除、临时排除、玩家豁免

**已释放路径**：
发布版本中累计声明已经放弃管理的历史托管文件路径。它属于完整目标状态，使从任意旧版本直接更新的玩家都能保留对应本地文件。
_避免使用_：一次性保留指令、删除例外

**默认文件**：
只在本地不存在时由发布版本提供的文件。文件一旦存在，其后续内容归玩家所有，不再被更新覆盖或删除。
_避免使用_：可合并文件、可选更新

**非托管文件**：
不属于目标文件清单、也不位于强制同步目录中的文件。玩家端更新器默认不修改它。
_避免使用_：忽略文件、多余文件

**强制同步目录**：
由管理端按整合包项目选择的一级目录，其本地内容必须递归收敛到发布版本声明的完整内容。不同目录可独立启用，未启用目录仍保留非托管文件。
_避免使用_：全盘同步、强制同步模式、黑名单目录

**强制同步文件**：
由管理端单独指定、必须始终与发布版本一致的一个托管文件。它不能被玩家本地豁免，但不会改变同目录其他文件的管理方式。
_避免使用_：强制同步目录、普通强制托管文件

**玩家本地豁免**：
玩家只在自己电脑上选择停止管理的文件、目录或模组组件。它优先于普通托管文件的安装、覆盖和删除，但不能绕过管理端声明的强制同步目录或强制同步文件。
_避免使用_：放弃管理、服务端排除规则

**玩家文件归档**：
强制同步目录中不属于目标发布版本的本地文件，在移出游戏实例后形成的玩家可恢复副本。归档由玩家自行管理，玩家端更新器不会自动清理。
_避免使用_：删除记录、事务临时备份、回收站

**内置文件源**：
由自托管管理端提供、供玩家端通过 HTTP 读取发布版本和文件内容的公共端点。它不承载管理凭据或私密数据。
_避免使用_：CDN、对象存储、管理接口

**管理控制端点**：
仅供服主在管理端所在电脑、可信内网或安全隧道中使用的管理界面与接口。它与面向玩家的内置文件源具有独立的访问边界。
_避免使用_：公共文件端点、公网后台

**项目签名身份**：
整合包项目独有的密码学身份，由管理端持有的私钥和玩家端信任的公钥组成。不同项目的签名身份彼此独立。
_避免使用_：管理账号、服务器密码

**项目绑定文件**：
随整合包首次分发、用于将一个玩家实例绑定到整合包项目的信任入口。它声明项目身份、公共文件地址和受信任的项目公钥；普通玩家界面只能更新本机玩家端安装目录，不能替换这些信任字段。
_避免使用_：玩家配置、登录凭据、在线注册

**玩家端安装目录**：
玩家端更新器的 JavaFX 程序、缓存、日志和本地状态所在的便携式目录。它由项目绑定中的实例相对路径指定，默认位于版本隔离后的游戏实例目录中；启动引导器和项目绑定文件始终留在实例内。
_避免使用_：固定系统目录、管理端目录

**可信发布版本**：
目标文件清单具有有效项目签名，且内容与签名一致的发布版本。仅有正确文件哈希但缺少有效签名的版本不属于可信发布版本。
_避免使用_：可下载版本、未损坏版本

**内容对象**：
由加密哈希唯一标识的不可变文件内容，可被不同发布版本或整合包项目安全复用。
_避免使用_：数据库附件、版本文件副本

**回滚发布**：
以某个历史发布版本的目标状态创建的新发布版本。它位于发布线末端，不修改或删除已经公开的历史记录。
_避免使用_：撤销发布、删除版本

**管理端备份**：
可在另一台机器上完整恢复管理端发布能力和既有文件服务的加密归档。它包含项目身份、发布历史、管理数据及全部内容对象。
_避免使用_：私钥导出、数据库副本、文件夹复制

**更新事务**：
玩家端将一次更新的暂存、校验、文件备份、替换和提交视为不可分割的过程。事务失败或中断时恢复原状态，成功提交后清理临时备份。
_避免使用_：边下边覆盖、长期版本备份

**启动引导器**：
在 Minecraft 主程序加载前运行的微型 Java Agent，负责启动玩家端更新器并等待启动许可。它不承担下载、文件安装或界面职责。
_避免使用_：玩家端更新器、模组加载器

**玩家端程序版本**：
可由启动引导器选择并运行的一份完整玩家端更新器程序。它独立于整合包发布版本，升级失败时可以回退到上一个已验证的程序版本。
_避免使用_：整合包版本、Bootstrap 版本

**可信玩家端程序版本**：
拥有有效项目签名的文件清单，且安装目录与该清单完全一致的玩家端程序版本。只有可信玩家端程序版本可以参与一次启动检查。
_避免使用_：能运行的 EXE、已下载更新器、当前目录
