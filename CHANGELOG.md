# DreamingFish Updater 更新日志

玩家端、管理端和 Bootstrap Agent 分别发布，版本号不会强制保持一致。本文件按组件独立记录；共用协议与更新引擎单列，避免把管理端能力误写成玩家端功能。

仓库早期迭代没有为每个发行版本建立独立提交。以下是截至 2026-07-27 的首次完整补录，覆盖项目从初版到当前发行版的累计演进；从下一版本开始，每次发行单独追加记录。

## 玩家端

### [0.1.19] - 2026-07-31

- 更新服务器维护或暂时不可达时，如果检测到的是整合包托管文件被玩家本地修改或删除，玩家端会显示“仍然启动”选项；二次确认后按当前本地文件继续启动，不会把未修复状态伪装成已验证安装。
- 签名清单、项目绑定、重放保护、危险路径、更新事务和玩家端程序自身损坏仍然不能绕过；只有可明确归类为本地托管内容变更的情况允许手动放行。
- 将本地内容变更与更新器状态损坏分开记录，日志会明确说明本次启动未修复本地托管文件。

### [0.1.18] - 2026-07-30

- 深化《初识梦屿：灯还亮着》的世界观表达，补充梦屿与旧世界的制度差异、外缘带的形成、逐光会成立时的现实矛盾，以及梁朔故事中的身份与记录主题。
- 保持入场新闻的叙事边界，不提前揭示逐光会后续变化、梁朔结局、感染研究结论或剧情反转。
- 玩家自选模组提示改为打开右侧详情小窗口中的“自选模组”标签，仅展示玩家添加的模组，并可在其中搜索、启用或停用。
- 模组风险提示按语义分为两行，并修复标签在较窄内容区域内无法完整换行的问题。

### [0.1.17] - 2026-07-30

- 新增一篇简短的“守望梦屿”入场导览，在同一篇新闻中介绍梦屿、外缘带、逐光会与梁朔，只保留玩家开场应知信息和必要悬念。
- 新闻详情返回列表时恢复进入前的滚动位置，浏览较靠后的新闻后无需重新寻找。
- 世界观新闻使用一张压缩封面，并增加篇幅与防剧透回归检查，避免后续编辑把阵营变化、研究结论或角色结局提前写入玩家端。

### [0.1.16] - 2026-07-30

- 新闻页改为“封面图、标题、日期和摘要”卡片列表，点击文章后在同一窗口阅读完整内容并可返回列表。
- 主页右上区域展示最新新闻的日期、标题和摘要，点击可直接打开完整文章；较矮窗口会自动隐藏入口，避免遮挡更新进度区。
- 统一主页右侧三个区域的圆角与对齐，新闻入口上移并让整体布局更靠近窗口右侧。
- “玩家自选模组”提示支持悬浮查看具体文件，点击可直接进入“本地文件 → 模组启停”侧栏管理。
- 收紧新闻列表卡片的封面、留白和高度；修复内置封面未显示，以及中文正文相邻时 Markdown 粗体标记泄漏为 `**` 的问题。
- 新闻采用 JSON 元数据与独立 Markdown 正文，支持标题、段落、强调、引用、列表、链接、代码、分隔线和正文图片，后续新增文章不再修改 Java 界面代码。
- 使用 CommonMark 解析 Markdown，不执行文章中的 HTML；新闻资源异常时显示稳定的空状态，不影响更新与 Minecraft 启动流程。

### [0.1.15] - 2026-07-29

- 撤回 `0.1.14` 的 Windows 原生窗口区域方案，主窗口和自定义弹窗恢复 `0.1.12` 使用的 JavaFX 透明舞台与场景圆角裁剪，消除原生圆角引入的黑色四角和弹窗边角问题。
- 完整移除 JNA 依赖及原生窗口处理代码，玩家端私有 Java 运行时同时移除不再需要的 `java.logging` 模块，降低后续玩家端更新体积。
- 恢复内置 `HarmonyOS Sans SC Bold` 作为界面首选字体，撤销额外的全局极粗字重，并将界面字号整体上调 `1px`；保留 `0.1.14` 的未验证离线启动和全部更新安全校验。

### [0.1.14] - 2026-07-29

- 移除对整棵 JavaFX 场景的圆角裁剪，改用 Windows 原生窗口区域处理普通窗口圆角；最大化时自动恢复矩形窗口，避免四角露出 Scene 黑底。
- 玩家端私有 Java 运行时显式加入 JNA 所需的 `java.logging` 模块，确保发布版 EXE 能实际应用原生窗口区域；保持现有 HarmonyOS 字体和液态玻璃视觉不变。
- 更新服务器无法连接且实例从未建立签名基线时，允许 Minecraft 继续启动，并明确显示“未验证离线启动”“本次未检查整合包”和“未执行文件验证”。
- 未验证放行只接受网络不可达；签名错误、清单或项目不匹配、重放、危险路径、事务损坏、下载哈希错误及已验证安装损坏仍会暂停启动。

### [0.1.13] - 2026-07-29

- 支持管理端“放弃管理并保留”语义：文件退出远程托管后不删除玩家本地副本，并在主页提示、悬浮摘要、本次更新详情和运行日志中列出保留路径。
- 支持服主对单个文件启用强制同步。被选文件不能由玩家使用文件豁免或模组停用绕过，但不会强制同步同目录中的其它内容。
- 保持同步优先级明确：目录级或单文件强制同步优先于玩家豁免；普通托管删除仍由玩家本地豁免优先。

### [0.1.12] - 2026-07-28

- 将关闭更新、恢复本地文件、停用模组、恢复默认和重启提示等确认弹窗统一为梦鱼界面的深色玻璃样式，移除 JavaFX 默认白色窗口与系统化按钮外观。
- 弹窗标题、对象名称和正文改为固定可用宽度下自动换行并按内容计算高度，不再以省略号截断；按钮统一使用完整中文操作名称。
- 弹窗宽度会随主窗口在合理范围内调整，并区分普通、风险和取消更新三种强调色，加入轻量淡入缩放动画。
- 预览模式中的本地文件与模组操作改用演示回调，确认弹窗后不再误调用尚未初始化的真实管理器。

### [0.1.11] - 2026-07-28

- 本地文件树改为“层级信息 + 固定管理按钮”两列布局。目录缩进只影响左侧文字，深层文件名和路径会在可用空间内截断，复选框始终固定在右侧并保持纵向对齐。

### [0.1.10] - 2026-07-28

- 窗口新增最大化与还原按钮，并支持双击标题栏切换；最大化时去除圆角裁切并禁用右下角手动缩放区域。
- 右侧更新与本地管理界面新增“铺满内容区 / 恢复侧栏”切换，文件管理、更新详情、历史和日志都可使用完整窗口宽度。
- 统一 ScrollPane、ListView 和 TreeView 的横纵滚动条为细窄青色样式，移除默认白色轨道、箭头和角落；文件树隐藏无用横向滚动条并为复选框保留固定空间。
- 本地文件和模组的管理行改为稳定版式；勾选后原位更新状态并保留文件树展开层级、滚动位置，避免列表上下跳动。
- “恢复全部管理”和“恢复整合包默认”成功后提示玩家关闭更新器，再从 MC 启动器重新启动游戏，并提供直接关闭更新器的按钮。

### [0.1.9] - 2026-07-27

#### 本地文件管理

- 新增“本地文件”抽屉，并分为“管理范围”和“模组启停”两种模式。
- 普通同步下，玩家可以只在本机豁免任意远程托管文件，也可以一次豁免整个目录；被修改的文件不再覆盖，被删除的文件不再反复下载。
- 文件规则保存到 `state/local-file-preferences.json`，模组启停规则保存到 `state/local-mod-preferences.json`，均不会上传到管理端。
- 重新启用某个目录的管理时，会清除该目录下冗余的单文件与子目录规则，避免留下互相冲突的状态。
- 本地规则采用路径边界匹配，例如豁免 `config/` 不会误伤 `configuration/`。
- 强制同步目录由服主策略优先：目录及子文件开关禁用，本地 JSON 规则也不能绕过；缺失文件会恢复，多余文件仍按强制同步规则移入备份。
- 保留独立的模组启停体验，支持整合包模组与玩家自选模组；停用的 JAR 移入 `local-mods/disabled/`，恢复时回到原路径。
- 读取 Forge、NeoForge 和 Fabric 元数据，以稳定组件 ID 记忆模组选择；模组升级并改名后仍能保持玩家选择，旧清单则退回精确路径匹配。
- 停用必要依赖前显示风险提示；强制同步目录中的模组不可停用。

#### 更新记录与详情

- 更新记录改为展示管理端提供的全部历史发布，并持久化本地缓存；断网或旧管理端没有历史接口时仍可显示当前签名发布和已有缓存。
- 修复中文更新记录在远程终端输入后显示为乱码、`P` 或重复占位字符所引发的客户端展示问题。
- 更新摘要支持鼠标悬浮预览变更文件，修复悬浮层反复出现和消失造成的闪烁。
- 点击更新摘要可打开“本次更新”标签，完整列出本轮安装、删除和移入备份的文件。
- 更新记录、运行记录和更新详情使用适合长列表的虚拟化展示，避免大量历史与文件记录拖慢界面。

#### 界面与交互

- 重做梦鱼服默认桌面界面，加入“主页、新闻、守望梦屿、关于”四个顶部栏目及选中下划线。
- 主页保留启动状态与右下角固定进度区；新闻、守望梦屿和关于页写入梦鱼服专属内容。
- 加入窗口入场动画、圆润大圆角、低亮度青紫发光边框和液态玻璃风格层次。
- 内置约 7.8 MiB 的 `HarmonyOS Sans SC Bold`，统一中文显示并提高更新信息可读性。
- 加入官网背景音乐及静音开关；保留沉浸式封面，不再显示服务器地址和无效头像占位。
- 顶部品牌改为“梦鱼服 DreamingFish”，英文使用青色与紫色区分；右下角显示 `DreamingFish Updater` 与玩家端版本。
- 日志抽屉重做为“运行记录”，更新历史、运行记录、本地文件和本次更新统一使用同一套抽屉交互。

#### 启动与更新流程

- Minecraft 通过 Java Agent 启动玩家端；更新检查通过后立即发放启动许可，不等待界面关闭。
- Minecraft 开始启动后显示顶部提示，并在 15 秒后自动关闭；玩家主动打开详情抽屉时保持窗口打开。
- 取消首次启动修改安装位置的询问，始终使用实例绑定中的相对 `playerHome`，整合包移动到其它电脑后仍可工作。
- 支持玩家端程序签名自更新、活动版本切换和失败回退；程序更新检查不可用时继续进行整合包检查。
- 支持离线启动最近一次完整验证的安装；签名、哈希、路径或事务状态不可信时拒绝发放启动许可。
- 额外模组在普通同步下只提醒、不删除且不阻止启动；强制同步归档时明确显示服主已启用策略、文件数量和备份位置。

## 管理端

### [0.1.16] - 2026-07-31

- 重做首次运行引导，明确说明 HTTP 监听地址、文件服务端口、Web 管理端口和玩家公网地址的用途、格式与必填要求；没有项目时可直接启动 Web 管理端继续创建。
- Web 创建项目表单补齐必填提示和风险说明，创建成功后自动进入“扫描与发布”；“标准源文件”更名为更直观的“整合包文件”。
- 整合包文件与单文件强制同步均支持按多级文件夹分类、文件夹递归勾选、半选状态、搜索后保持选择和当前列表全选。
- 支持批量移除整合包文件，后端统一完成校验、归档、删除和扫描，执行中失败会恢复已经移出的文件。
- 将发布预览移动到单文件强制同步下方；扫描没有差异时明确显示“本次没有修改”。
- 修复回滚与创建项目弹窗中，必填字段为空时点击“取消”仍触发浏览器校验、无法关闭弹窗的问题。
- SSH 隧道提示统一为完整的 `ssh -N -L 18080:127.0.0.1:18080 用户名@您的服务器地址` 示例。

### [0.1.15.1] - 2026-07-31

- Linux 发行包的 `dfs-admin` 启动脚本内置 `-Djava.net.preferIPv4Stack=true`，避免 Java 在部分 Linux 环境中只创建 IPv6 通配监听，导致 IPv4 NAT、端口映射或反向代理无法连接公共 HTTP 服务。
- 服主直接运行 `./dfs-admin` 即可获得 IPv4 监听，不再需要手动设置 `JAVA_TOOL_OPTIONS`；Windows 管理端行为不变。
- 发行构建会检查 Linux 启动脚本必须包含该参数，防止后续打包时回归。

### [0.1.15] - 2026-07-29

- 取消 Web 左侧独立“源文件”入口，把标准源文件搜索、上传、导入、移除和策略查看完整合并到“扫描与发布”页顶部，日常发布流程集中在一个页面完成。
- 限制组合页面中的源文件表格高度，保留独立滚动区域，避免长文件列表把预览、强制同步设置和发布表单推得过远。
- Web 启动的公共 HTTP 文件服务会在发布整合包或创建回滚发布后自动停止并重新绑定；页面明确提示重启成功。
- 如果版本已经不可变发布但 HTTP 服务重启失败，API 仍返回发布成功并携带独立警告，避免服主误以为发布失败后重复创建版本。
- Web 默认最低玩家端版本更新为 `0.1.14`，并新增运行中发布、自动重启、健康检查及最新清单切换的集成测试。

### [0.1.14] - 2026-07-29

#### Web 源文件管理

- 新增“源文件”页面，可搜索并查看标准源目录中的全部托管文件、大小、修改时间、发布状态，以及普通托管、目录强制和单文件强制策略。
- 支持从浏览器拖入或多选文件上传，并显示整批上传进度；多文件上传只在整批结束后扫描一次，避免每个文件重复扫描整合包。
- 支持调用管理服务器本机的 Windows 文件选择窗口导入文件；选择窗口使用置顶所有者，避免弹到浏览器或远程桌面后方。
- 覆盖或移除源文件前会把原文件校验后归档到 `data/source-archive/<项目>/<批次>/`，便于服主恢复误操作。
- 移除已发布文件时直接选择“从玩家端删除”或“放弃管理并保留”；强制同步目录内禁止放弃管理，规则与 CLI 完全一致。
- 修复单文件强制同步保存后仍显示“普通托管”的状态反馈；勾选时只更新当前行与计数，不重排列表。
- 修复尚未生成扫描预览时，Web 文件列表把实际存在的强制同步文件误报为“源文件缺失”的问题。

#### 首次部署与玩家端发布

- 新增“首次部署包”：管理端不需要完整 Minecraft 实例，即可从已发布对象重建签名玩家端、Bootstrap Agent、项目绑定、封面和指定历史版本的签名基线。
- 首次部署包是用于合并到服主本地实例的薄目录，不复制 `mods/`、`config/` 或 Minecraft 本体；管理端 Windows/Linux 发行包均内置所需 `support/bootstrap-agent.jar`。
- 修复薄首次部署包仍创建空强制同步目录的问题，部署包现在只保留更新器运行与签名校验所需内容。
- 发布玩家端程序时只需选择发行包解压根目录；管理端会读取 `active-player.properties`、语义版本目录或 jpackage 元数据，并自动定位唯一启动 EXE。

#### 项目、迁移与 Windows 交互

- Web、交互终端和参数式 CLI 均可修改项目显示名称，旧项目中已经保存的乱码名称可以直接修正。
- 管理端迁移到新目录后，如果 EXE 同级存在有效的 `data/management.db`，会自动优先使用并修正设置中的旧绝对路径；空 `data/` 不会触发误迁移。
- Windows CLI 使用原生控制台控制处理器拦截 `Ctrl+C` 和 `Ctrl+Break`，停止 HTTP 或 Web 服务后返回主菜单，不再连同整个管理端窗口一起退出。
- 玩家端没有改动，版本继续保持 `0.1.13`。

#### 验证

- 新增源文件上传、导入、覆盖归档、删除/放弃管理、强制目录约束、项目改名、玩家端目录自动识别、薄部署包、管理端搬家和 Windows 集成测试。
- 发行包冒烟测试新增管理端内置 Agent、玩家端根目录自动发布和无完整实例首次部署包的端到端校验。

### [0.1.13.1] - 2026-07-29

- 修复 Windows CMD/ConHost 使用代码页 936 时，管理端强制输出 UTF-8 导致整个中文菜单显示为 `鏁村悎鍖...` 的问题。
- 交互式 Windows 控制台现在读取实际代码页：CP936 使用 GBK 输出，CP65001 使用 UTF-8，其它代码页使用对应 Java Charset，并在无法识别时回退到系统原生编码。
- 输出被管道、脚本或文件重定向时仍固定为 UTF-8；Web 页面、JSON、数据库、清单和日志的 UTF-8 编码保持不变。
- 玩家端没有相关改动，版本继续保持 `0.1.13`。

### [0.1.13] - 2026-07-29

#### 发布与文件生命周期

- 删除标准源目录中的已发布文件后，管理端不再直接假定玩家端删除；发布前必须逐项选择“删除玩家文件”或“放弃管理并保留”，Web 页面支持逐项及批量设置。
- 放弃管理的路径会累计写入以后每个完整发布，旧整合包可直接跨多个版本更新到最新版而不会误删应保留文件；文件重新放回标准目录时自动恢复托管。
- 参数式发布新增 `--removed-files DELETE|RELEASE`，用于为尚未决定的移除项统一选择操作。
- 强制同步目录中的移除文件禁止放弃管理，避免与目录完整镜像规则冲突。

#### 强制同步与 Web 管理

- 新增单文件强制同步，可在 Web 页面搜索、勾选并保存具体托管文件；同目录其它文件仍保持普通同步。
- CLI 新增 `--force-sync-files` 与 `--clear-force-sync-files`，扫描会验证目标文件真实存在、未被排除且路径大小写一致。
- 项目源目录、玩家端 app-image、Minecraft 实例和封面路径旁新增“浏览”按钮，由管理端调用 Windows 原生文件或目录选择窗口。

#### Windows 启动与中文编码

- Windows 管理端改为原生控制台入口 `DreamingFishAdmin.exe`，不再依赖 `dfs-admin.cmd`，管理数据仍固定保存在 EXE 所在目录的 `data/`。
- Java 文件与重定向输出固定为 UTF-8；控制台输入优先严格解析 UTF-8，并为旧版 Windows Server 增加 GB18030 回退。
- 签名清单和内容对象的原子移动会短暂重试 Windows 杀毒或索引服务造成的瞬时文件锁，持续失败时仍安全中止发布。
- 已写入数据库的 `��` 或问号无法反向恢复，升级后需要重新填写相关更新记录。

### [0.1.9] - 2026-07-28

#### Web 管理界面

- 新增桌面优先的深色 Web 管理界面，覆盖项目切换与创建、项目和品牌设置、扫描预览、不可变发布、发布历史、回滚、玩家端程序发布、玩家实例制作及服务设置。
- 概览页可直接启动和停止面向玩家的公共 HTTP 文件服务；页面不轮询后台，空闲时没有持续数据库查询或网络请求。
- Web 后端使用 JDK 内置 `HttpServer` 和固定 4 个守护请求线程，不引入 Node、Electron 或常驻 Web 框架，适合资源有限的服务器。
- Web 管理端固定监听 `127.0.0.1:18080`，拒绝绑定公网地址；远程管理通过 SSH 本地端口转发访问。
- 所有写操作要求随机临时会话令牌，并加入同源资源策略、禁止嵌入、请求体大小限制及串行管理操作锁。
- 加密备份和恢复继续只允许从 CLI 执行，避免高风险运维动作暴露在浏览器入口。

#### 终端与配置

- 主菜单新增 `[10] 启动 Web 管理界面`；`[8]` 统一管理公共 HTTP 监听地址、公共端口和本机 Web 端口。
- 交互菜单中的 HTTP 文件服务和 Web 管理服务支持按 `Ctrl+C` 停止当前服务并返回主菜单，不再强制退出整个管理端。
- 管理设置升级到 schema 2；旧 schema 1 设置自动迁移并获得默认 Web 端口 `18080`，公共 HTTP 端口与 Web 端口禁止重复。
- 公共下载服务与 Web 管理服务使用独立监听器：默认分别为 `0.0.0.0:8080` 和 `127.0.0.1:18080`。
- 发行脚本新增 `-AdminOnly`，更新管理端时不再额外构建一个同版本号的玩家端包。

#### 验证

- 新增 Web 集成测试，覆盖静态资源与安全响应头、无会话令牌拒绝、项目创建、扫描、发布、历史读取及公共 HTTP 服务启停。
- 新增旧设置迁移测试，并在桌面视口完成创建项目、扫描、确认发布、发布历史和设置页的浏览器验证。

### [0.1.8] - 2026-07-27

#### 部署与交互

- 提供 Windows 与 Linux x64 自带 Java 运行时的命令行发行包，以及中文交互终端和首次启动向导。
- 管理数据固定保存在管理端根目录的 `data/`，不再要求服主配置内部数据目录。
- 标准整合包源目录只管理实际存在的内容，不强制创建 `mods/` 或 `config/`；服主可以只管理任意一个或多个所需目录。
- HTTP 默认监听 `0.0.0.0:8080`，项目公共地址可独立填写公网域名、IP 与映射端口。
- 支持项目创建、状态查看、设置修改、发布历史、玩家端程序发布、玩家实例制作、HTTP 设置和只读文件服务等交互菜单。

#### 发布能力

- 扫描源目录并预览新增、修改、删除、策略变化和模组元数据变化，确认后生成不可变签名发布。
- 发布记录支持中文、多行内容和 UTF-8 文本文件输入；交互模式使用 `@文件路径`，参数模式使用 `--changelog-file`，发布前回显实际保存内容。
- 增加只读发布历史接口，供玩家端展示所有历史版本及更新说明。
- 扫描 Forge、NeoForge 与 Fabric JAR，向清单写入稳定组件 ID 和显示名称。
- 可按一级目录独立启用强制同步；强制目录缺失时拒绝发布，显式空目录可发布“清空该目录”的目标状态。
- 发布玩家端完整 app-image 并签名，制作玩家实例时写入项目绑定、玩家端状态和指定历史版本的签名分发基线。
- 制作玩家实例时清理本机缓存、日志、事务、备份、模组启停和文件豁免偏好，避免把服主测试状态发给玩家。

#### 服务与运维

- 只读 HTTP 服务提供健康检查、签名发布、内容对象、玩家端程序及发布历史；支持 `Range`、`ETag` 和缓存头。
- 管理端以 SQLite 保存项目和发布元数据，以 SHA-256 内容寻址对象保存文件，相同内容只存一份。
- 提供密码保护的管理数据备份与恢复流程，保留项目私钥、身份和完整发布历史。
- 发行构建会校验管理端实际报告版本；冒烟测试支持玩家端 `0.1.9` 与管理端 `0.1.8` 分别指定版本。

## Bootstrap Agent

### [0.1.2] - 2026-07-27

- 保持 Java 8 兼容，在 Minecraft JVM 真正进入游戏前启动独立玩家端并等待许可。
- 通过仅监听回环地址的短生命周期通道与随机凭据收取许可，不向公网暴露控制端口。
- 启动活动玩家端和回退版本前都校验签名清单、项目身份与程序目录完整性，篡改时拒绝启动。
- 使用实例内相对玩家端路径，兼容 PCL 的版本隔离实例和整合包整体迁移。
- Agent 不在线自更新；要求更高 Agent 版本时必须重新制作并分发完整玩家实例。

## 共用协议与更新引擎

### [0.1.13 兼容更新] - 2026-07-29

- 发布清单新增 `releasedPaths`，以完整目标状态携带已放弃管理的历史路径；重新托管时会从该列表移除。
- 发布清单新增 `forcedSyncFiles`，只对精确规范路径启用强制管理，不扩散到父目录或相邻文件。
- 新增必要能力 `released-paths-v1` 与 `forced-file-sync-v1`；旧玩家端遇到不能理解的新语义时会明确拒绝发布，而不是静默执行错误策略。
- 清单验证新增排序、去重、路径冲突、强制文件存在性和文件策略检查，禁止同一路径同时处于托管、放弃管理或冲突强制状态。
- 更新规划器在跨版本更新时区分删除和放弃管理；普通删除继续尊重玩家本地豁免，单文件和目录强制同步继续覆盖本地豁免。

### [0.1.9 兼容更新] - 2026-07-27

- 发布清单增加可选模组组件 ID 与显示名称字段，并保持旧清单兼容。
- 新增发布历史协议和玩家端历史缓存模型。
- 新增玩家本地单文件、目录与组件豁免模型；规划器在普通同步中跳过豁免目标，在强制同步中明确覆盖本地规则。
- 更新结果记录完整的安装、删除与归档路径，为玩家端“本次更新”详情提供可靠数据。
- 保持按 SHA-256 对象增量下载：跨多个整合包版本直接比较本地已验证内容与最新完整清单，只下载本机缓存中缺失的对象。
- 任意旧正式整合包通过签名分发基线直接收敛到最新版本，已删除的旧官方文件不会被误判为玩家自选文件。
- 普通 `ENFORCED` 文件会修复缺失或损坏内容，但不会清理远程清单外的玩家额外文件；`DEFAULT` 文件仅在缺失时安装。
- 强制同步可按一级目录配置，远程不存在的额外文件先进入事务归档，整次更新成功后再提交到长期备份。
- 文件安装、删除、归档和状态提交使用持久化事务与原子替换；下载中断、进程崩溃或校验失败时可恢复或回滚。
- 强化 Windows 路径大小写、目录边界、符号链接、保留名称和实例逃逸校验，并在 Minecraft 正在运行时拒绝更新。
