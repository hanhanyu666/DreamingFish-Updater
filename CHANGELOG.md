# DreamingFish Updater 更新日志

玩家端、管理端和 Bootstrap Agent 分别发布，版本号不会强制保持一致。本文件按组件独立记录；共用协议与更新引擎单列，避免把管理端能力误写成玩家端功能。

仓库早期迭代没有为每个发行版本建立独立提交。以下是截至 2026-07-27 的首次完整补录，覆盖项目从初版到当前发行版的累计演进；从下一版本开始，每次发行单独追加记录。

## 玩家端

### [0.1.40] - 2026-08-14

- 首页欢迎文字与大号主标题支持单独设置文字颜色，旧项目默认保持原有的米白色外观。

### [0.1.39] - 2026-08-14

- 窗口顶栏恢复原来的半透明外观，旧项目与未配置项目默认使用 22% 底色不透明度，不再因深色主题底色过重而显示成黑色实条。
- 支持读取管理端保存的顶栏底色不透明度，并在实时个性化配置、离线缓存和整合包发布配置之间保持一致。

### [0.1.38] - 2026-08-14

- 打开“运行记录”时会直接定位到最新一条；停留在底部时继续跟随新增日志，玩家主动向上翻看旧记录后不会被强行拉回。
- 首页“欢迎来到”支持按项目自定义；窗口顶栏和各类布局卡片也可以分别设置主题颜色，旧项目继续使用原来的深色默认外观。
- 玩家端个性化协议会保留普通页面正文与公告草稿两套内容，页面当前显示哪一种不再决定另一种内容是否被删除。

### [0.1.37] - 2026-08-13

- 本地运行日志现在完整记录日期、时间、级别和所属阶段；每次启动都有清楚的分隔，旧版没有日期的记录也能继续查看。
- “运行记录”按日期整理日志，并区分普通、提醒和错误；异常堆栈默认收起，需要排查时可以展开查看，不再把整页挤成一大段文字。
- 玩家端日志与界面侧车日志都会自动轮换，只保留有限数量的历史文件，避免长期使用后日志无限占用磁盘空间。
- 更新检查、下载、安装、恢复和自更新等阶段改为更容易理解的中文提示，玩家可以直接看出当前正在处理什么。

### [0.1.36] - 2026-08-10

- “本地文件”中的文件管理范围与模组启停改为带用途说明的双卡片切换，搜索、恢复按钮和文件列表在最小窗口下保持清晰对齐。
- 打开本地管理抽屉时，启动状态提示会自动让开标题与标签页；铺满内容区时不再覆盖抽屉操作。

### [0.1.35] - 2026-08-10

- 签名清单、玩家端程序清单和个性化配置在静态托管没有自定义响应头时，会读取同路径的 `.sig` 签名文件；普通 HTTP、对象存储与 CDN 无需运行 DreamingFish 服务端程序也能安全分发更新。
- 仍优先验证管理端原有的 `X-Dfs-Signature` 响应头；响应头存在但签名无效时不会降级改信 `.sig`，避免错误配置掩盖签名异常。

### [0.1.34] - 2026-08-09

- 标题、副标题、品牌名称、配色、服务器地址、公告和自定义页面改为启动时读取管理端最新签名配置；服主保存后无需创建新的整合包版本，玩家下次启动即可看到。
- 玩家端会在本地保存最后一次签名验证成功的个性化配置，管理端离线时继续显示上次内容；无效签名和损坏缓存不会被采用。
- 背景图片和 MP3 继续使用正式发布清单增量同步，避免把大文件资源混入高频个性化配置。

### [0.1.33] - 2026-08-09

- 玩家按下启动游戏后，一收到检查进度就立即显示玩家端窗口；网络较慢、正在下载玩家端自更新时不再长时间没有任何反馈。
- 自更新阶段沿用上一次成功发布的标题、副标题、配色与页面内容，并把右下角进度条上方的阶段文字改为“玩家端正在自更新”；窗口显示后与普通更新一样播放音乐。

### [0.1.32] - 2026-08-09

- 修复自更新检查期间窗口尚未显示但音乐已经播放的问题；旧进程在准备切换版本时保持静音，不再出现音乐中断后从头播放。
- 启动音乐只会在原生窗口成功显示后启用，窗口显示失败或初始化错误时不会在后台播放声音。

### [0.1.31] - 2026-08-09

- 内容页中的音乐播放器移动到标题栏右上角窗口按钮左侧；展开控制面板时自动放到标题栏下方，不再挡住新闻和公告正文。
- 移除内容页底部播放器预留空白，保持正文区域完整可用。

### [0.1.30] - 2026-08-09

- 修复玩家端自更新时旧窗口先显示、更新后新窗口再次显示的问题；更新器现在会在程序版本检查完成后再显示界面，升级切换过程不再闪现两次窗口。
- 修复启动品牌在绑定文件、已安装发布清单和更新结果之间来回覆盖的问题；首次显示直接使用上一次成功发布的品牌内容，启动标题不会再出现“梦屿 / 服务器名称”跳变。
- 初始化或侧车异常时仍会正常显示窗口和错误信息，不会因为启动窗口延后显示而变成后台进程。

### [0.1.29] - 2026-08-08

- 新增项目音乐播放卡片：收起时只保留紧凑的当前曲目提示，点击后展开曲目选择、播放/暂停和循环播放控制，不再占用标题栏导航空间；循环默认关闭并记住玩家选择。
- 支持从项目发布清单下载多首 MP3 到独立音乐目录，单首音乐可选播放；音乐缺失或下载失败不会阻止整合包更新和游戏启动。
- 保留旧版启动音乐路径兼容，并在玩家端限制音乐文件路径、格式和大小，避免音乐配置影响实例其它文件。

### [0.1.28] - 2026-08-04

- 顶部导航改为项目化页面：固定保留“主页”和“关于更新器”，公告、新闻、规则与玩法页面全部由服主创建、删除和排序；没有公告页时主页不再显示新闻入口。
- 公告页支持多条文章，并从全部公告页中选取最新文章展示在主页；普通内容页与公告正文继续使用相同的 Markdown 排版。
- 顶部导航在页面较多时可横向滚动，避免挤压窗口按钮或截断品牌名称。
- 玩家端落盘的设置、安装状态和事务记录改为带缩进的易读 JSON，更新协议与逐行通信格式保持不变。

### [0.1.27] - 2026-08-03

- 玩家端窗口左上角的中文与英文品牌名称现在可以按整合包项目配置；管理端 Web、交互终端与参数式命令均可设置，旧项目自动沿用“梦鱼服 / DreamingFish”。
- 标题栏会安全处理空值和过长文字，品牌名称过长时自动省略，不影响窗口拖动和导航按钮。
- 新闻内容与顶部自定义页面改由项目配置提供，不再把梦鱼服专属介绍写死在通用玩家端中；旧项目仍可继续显示程序内置新闻。
- “关于”页面改为介绍 DreamingFish Updater，显示玩家端程序版本，并提供项目 GitHub 仓库入口。
- 支持管理端将品牌、新闻、自定义页面和背景即时投射到与本地玩家端相同的 Vue 界面中进行预览。

### [0.1.26] - 2026-08-02

- 修复 Tauri 自定义标题栏无法拖动窗口的问题：补充窗口拖动权限，并让标题栏空白区域成为拖动区域；导航以及最小化、最大化和关闭按钮仍可正常点击。

### [0.1.25] - 2026-08-02

- 恢复优先使用玩家端内置的鸿蒙字体，不再被系统微软雅黑覆盖。
- 首页整合包名称改用与“欢迎来到”一致的轻阴影，避免此前过重、下坠的重影。

### [0.1.24] - 2026-08-01

- 修复 Tauri 侧车命令循环的确认死锁：确认回复由持续运行的标准输入读取线程立即处理，其他业务命令交给单线程保序执行，关闭、恢复文件等操作不再卡在无法读取自身确认结果的状态。
- 移除前端关闭更新器时的重复确认，统一由 Java 控制器根据真实更新状态决定是否询问，避免连续弹出两次相同对话框。
- 本地操作确认与 Java 启动确认改为排队显示，后到确认不会覆盖前一个对话框或遗留永不完成的 Promise。
- Java 已放行 Minecraft 后，本地模组/文件管理列表刷新失败只记录警告，不再错误显示“启动已暂停”。
- Java 侧车异常关闭时保留 Tauri 窗口，汇总并截断错误输出供复制和重试；正常关闭统一使用显式 `exit` 消息。
- 修复本地背景路径转换竞态；自定义封面改由 Rust 在确认文件位于当前实例、格式和大小合法后读取，不开放浏览器任意本地文件权限。
- 侧车启动失败后，“重试”会重新启动 Java 更新引擎；关闭指令通道损坏时窗口仍可强制退出，Alt+F4 也会优先交给 Java 执行业务确认。
- 调整确认对话框的信息层级、正文宽度、留白和按钮布局，补充确认请求队列、焦点限制、焦点恢复、Escape 取消、键盘焦点样式、可复制文本和减少动态效果支持。
- 修复文件树搜索无法展开曾折叠父目录、主页最新新闻首次进入只显示列表的问题，并补充 Windows 程序图标。
- 发布构建不再接受 `DFS_JAVA` 或 `DFS_SIDECAR_JAR` 环境变量覆盖，只执行发行目录内受程序清单保护的内置运行时和侧车；开发构建仍保留覆盖能力。
- 发布构建会校验 Java、Vue、npm、Cargo 与 Tauri 版本一致，并兼容 Windows PowerShell 5.1 与 PowerShell 7。
- 侧车调试日志只记录参数名，不记录 Bootstrap token、实例路径或玩家名称等参数值。

### [0.1.23] - 2026-08-01

- 修复 Tauri 玩家端启动 Java 侧车后立即报错的问题：控制器现在会在使用前创建 Bootstrap 启动许可客户端，能够正常向启动代理发送就绪信号并继续检查更新。
- 修复初始化失败时窗口关闭按钮失效的问题；错误状态下可直接退出，其他拒绝启动路径也保证最终关闭窗口。

### [0.1.22] - 2026-08-01

- 修复玩家从 0.1.19 升级后 PCL 无法启动的问题：Windows UAC 的“安装程序检测”会把名为 `DreamingFishUpdater.exe` 且清单未声明执行级别的程序误判为需要管理员权限，非管理员进程用 `CreateProcess` 启动时返回错误 740（请求的操作需要提升）。
- Tauri 启动器清单显式声明 `requestedExecutionLevel="asInvoker"`，与旧版 jpackage 启动器行为一致，普通权限即可直接启动。

### [0.1.21] - 2026-08-01

- 玩家端界面从 JavaFX 迁移到 Tauri 2 + Vue 3 + TypeScript：窗口外壳由 Rust 提供，界面使用 WebView2 渲染，保留原有“主页、新闻、守望梦屿、关于”导航和全部布局元素；窗口透明圆角、白天可读性、新闻滚轮和复选框图标渲染均已修正。
- 更新引擎继续复用现有 Java 代码，作为 sidecar 进程运行；Rust 外壳负责启动 Java 侧车并在前后端之间转发 JSON 行协议，Minecraft 启动许可、离线策略、本地文件/模组管理和更新事务逻辑保持不变。
- 原有 JavaFX 玩家端保留为回退路径（`build-distributions.ps1` 默认仍构建旧版），新界面通过 `-TauriPlayer` 参数构建；完整迁移并验证后切换默认。
- 新增 Vitest 前端单元测试（格式化、目录树、Markdown 渲染、新闻加载、全局状态）和 Java 侧车协议测试（预览消息序列、控制器错误处理），现有全量 Maven 测试保持通过。

### [0.1.20] - 2026-08-01

- 玩家文件下载改为复用同一个 HTTP 客户端，并默认同时下载最多 4 个独立对象；可通过 `dfs.download.parallelism` 在 1 到 8 之间调整。
- 玩家端只保留当前程序和上一个可回退程序，并同步清理更旧的程序签名清单；成功安装后自动清空可重新下载的对象缓存。
- 中断下载分片保留 7 天供断点续传，过期后自动清理；不会自动清理 Minecraft 实例文件或强制同步备份。
- 移除背景音乐、音乐按钮、音频资源和 JavaFX Media 依赖，保留内置 HarmonyOS 字体。
- 发行包按目标平台仅保留对应的 SQLite 原生库，管理端运行时移除不需要的通用 Java SE 模块；Linux 包移除可选的预生成 CDS 启动缓存。
- 随玩家端分发的 Bootstrap Agent 改为最小化打包并自动校验 Java 8 签名验证能力，Agent 体积约减少 51%，保持 Java 8 兼容。

### [0.1.19] - 2026-07-31

- 更新服务器维护或暂时不可达时，如果检测到的是整合包托管文件被玩家本地修改或删除，玩家端会显示“仍然启动”选项；二次确认后按当前本地文件继续启动，不会把未修复状态伪装成已验证安装。
- 签名清单、项目绑定、重放保护、危险路径、更新事务和玩家端程序自身损坏仍然不能绕过；只有可明确归类为本地托管内容变更的情况允许手动放行。
- 将本地内容变更与更新器状态损坏分开记录，日志会明确说明本次启动未修复本地托管文件。

### [0.1.18] - 2026-07-30

- 深化《初识梦屿：灯还亮着》的世界观表达，补充梦屿与旧世界的制度差异、外缘带的形成、逐光会成立时的现实矛盾，以及梁朔故事中的身份与记录主题。
- 保持入场新闻的叙事边界，不提前揭示逐光会后续变化、梁朔结局、感染研究结论或剧情反转。
- 玩家自选模组提示改为打开右侧详情小窗口中的“自选模组”标签，仅展示玩家添加的模组，并可在其中搜索、启用或停用。
- 模组风险提示按语义分为两行，并修复标签在较窄内容区域内无法完整换行的问题。

### [0.1.17] - 2026-07-30

- 新增一篇简短的“守望梦屿”入场导览，在同一篇新闻中介绍梦屿、外缘带、逐光会与梁朔，只保留玩家开场应知信息和必要悬念。
- 新闻详情返回列表时恢复进入前的滚动位置，浏览较靠后的新闻后无需重新寻找。
- 世界观新闻使用一张压缩封面，并增加篇幅与防剧透回归检查，避免后续编辑把阵营变化、研究结论或角色结局提前写入玩家端。

### [0.1.16] - 2026-07-30

- 新闻页改为“封面图、标题、日期和摘要”卡片列表，点击文章后在同一窗口阅读完整内容并可返回列表。
- 主页右上区域展示最新新闻的日期、标题和摘要，点击可直接打开完整文章；较矮窗口会自动隐藏入口，避免遮挡更新进度区。
- 统一主页右侧三个区域的圆角与对齐，新闻入口上移并让整体布局更靠近窗口右侧。
- “玩家自选模组”提示支持悬浮查看具体文件，点击可直接进入“本地文件 → 模组启停”侧栏管理。
- 收紧新闻列表卡片的封面、留白和高度；修复内置封面未显示，以及中文正文相邻时 Markdown 粗体标记泄漏为 `**` 的问题。
- 新闻采用 JSON 元数据与独立 Markdown 正文，支持标题、段落、强调、引用、列表、链接、代码、分隔线和正文图片，后续新增文章不再修改 Java 界面代码。
- 使用 CommonMark 解析 Markdown，不执行文章中的 HTML；新闻资源异常时显示稳定的空状态，不影响更新与 Minecraft 启动流程。

### [0.1.15] - 2026-07-29

- 撤回 `0.1.14` 的 Windows 原生窗口区域方案，主窗口和自定义弹窗恢复 `0.1.12` 使用的 JavaFX 透明舞台与场景圆角裁剪，消除原生圆角引入的黑色四角和弹窗边角问题。
- 完整移除 JNA 依赖及原生窗口处理代码，玩家端私有 Java 运行时同时移除不再需要的 `java.logging` 模块，降低后续玩家端更新体积。
- 恢复内置 `HarmonyOS Sans SC Bold` 作为界面首选字体，撤销额外的全局极粗字重，并将界面字号整体上调 `1px`；保留 `0.1.14` 的未验证离线启动和全部更新安全校验。

### [0.1.14] - 2026-07-29

- 移除对整棵 JavaFX 场景的圆角裁剪，改用 Windows 原生窗口区域处理普通窗口圆角；最大化时自动恢复矩形窗口，避免四角露出 Scene 黑底。
- 玩家端私有 Java 运行时显式加入 JNA 所需的 `java.logging` 模块，确保发布版 EXE 能实际应用原生窗口区域；保持现有 HarmonyOS 字体和液态玻璃视觉不变。
- 更新服务器无法连接且实例从未建立签名基线时，允许 Minecraft 继续启动，并明确显示“未验证离线启动”“本次未检查整合包”和“未执行文件验证”。
- 未验证放行只接受网络不可达；签名错误、清单或项目不匹配、重放、危险路径、事务损坏、下载哈希错误及已验证安装损坏仍会暂停启动。

### [0.1.13] - 2026-07-29

- 支持管理端“放弃管理并保留”语义：文件退出远程托管后不删除玩家本地副本，并在主页提示、悬浮摘要、本次更新详情和运行日志中列出保留路径。
- 支持服主对单个文件启用强制同步。被选文件不能由玩家使用文件豁免或模组停用绕过，但不会强制同步同目录中的其它内容。
- 保持同步优先级明确：目录级或单文件强制同步优先于玩家豁免；普通托管删除仍由玩家本地豁免优先。

### [0.1.12] - 2026-07-28

- 将关闭更新、恢复本地文件、停用模组、恢复默认和重启提示等确认弹窗统一为梦鱼界面的深色玻璃样式，移除 JavaFX 默认白色窗口与系统化按钮外观。
- 弹窗标题、对象名称和正文改为固定可用宽度下自动换行并按内容计算高度，不再以省略号截断；按钮统一使用完整中文操作名称。
- 弹窗宽度会随主窗口在合理范围内调整，并区分普通、风险和取消更新三种强调色，加入轻量淡入缩放动画。
- 预览模式中的本地文件与模组操作改用演示回调，确认弹窗后不再误调用尚未初始化的真实管理器。

### [0.1.11] - 2026-07-28

- 本地文件树改为“层级信息 + 固定管理按钮”两列布局。目录缩进只影响左侧文字，深层文件名和路径会在可用空间内截断，复选框始终固定在右侧并保持纵向对齐。

### [0.1.10] - 2026-07-28

- 窗口新增最大化与还原按钮，并支持双击标题栏切换；最大化时去除圆角裁切并禁用右下角手动缩放区域。
- 右侧更新与本地管理界面新增“铺满内容区 / 恢复侧栏”切换，文件管理、更新详情、历史和日志都可使用完整窗口宽度。
- 统一 ScrollPane、ListView 和 TreeView 的横纵滚动条为细窄青色样式，移除默认白色轨道、箭头和角落；文件树隐藏无用横向滚动条并为复选框保留固定空间。
- 本地文件和模组的管理行改为稳定版式；勾选后原位更新状态并保留文件树展开层级、滚动位置，避免列表上下跳动。
- “恢复全部管理”和“恢复整合包默认”成功后提示玩家关闭更新器，再从 MC 启动器重新启动游戏，并提供直接关闭更新器的按钮。

### [0.1.9] - 2026-07-27

#### 本地文件管理

- 新增“本地文件”抽屉，并分为“管理范围”和“模组启停”两种模式。
- 普通同步下，玩家可以只在本机豁免任意远程托管文件，也可以一次豁免整个目录；被修改的文件不再覆盖，被删除的文件不再反复下载。
- 文件规则保存到 `state/local-file-preferences.json`，模组启停规则保存到 `state/local-mod-preferences.json`，均不会上传到管理端。
- 重新启用某个目录的管理时，会清除该目录下冗余的单文件与子目录规则，避免留下互相冲突的状态。
- 本地规则采用路径边界匹配，例如豁免 `config/` 不会误伤 `configuration/`。
- 强制同步目录由服主策略优先：目录及子文件开关禁用，本地 JSON 规则也不能绕过；缺失文件会恢复，多余文件仍按强制同步规则移入备份。
- 保留独立的模组启停体验，支持整合包模组与玩家自选模组；停用的 JAR 移入 `local-mods/disabled/`，恢复时回到原路径。
- 读取 Forge、NeoForge 和 Fabric 元数据，以稳定组件 ID 记忆模组选择；模组升级并改名后仍能保持玩家选择，旧清单则退回精确路径匹配。
- 停用必要依赖前显示风险提示；强制同步目录中的模组不可停用。

#### 更新记录与详情

- 更新记录改为展示管理端提供的全部历史发布，并持久化本地缓存；断网或旧管理端没有历史接口时仍可显示当前签名发布和已有缓存。
- 修复中文更新记录在远程终端输入后显示为乱码、`P` 或重复占位字符所引发的客户端展示问题。
- 更新摘要支持鼠标悬浮预览变更文件，修复悬浮层反复出现和消失造成的闪烁。
- 点击更新摘要可打开“本次更新”标签，完整列出本轮安装、删除和移入备份的文件。
- 更新记录、运行记录和更新详情使用适合长列表的虚拟化展示，避免大量历史与文件记录拖慢界面。

#### 界面与交互

- 重做梦鱼服默认桌面界面，加入“主页、新闻、守望梦屿、关于”四个顶部栏目及选中下划线。
- 主页保留启动状态与右下角固定进度区；新闻、守望梦屿和关于页写入梦鱼服专属内容。
- 加入窗口入场动画、圆润大圆角、低亮度青紫发光边框和液态玻璃风格层次。
- 内置约 7.8 MiB 的 `HarmonyOS Sans SC Bold`，统一中文显示并提高更新信息可读性。
- 加入官网背景音乐及静音开关；保留沉浸式封面，不再显示服务器地址和无效头像占位。
- 顶部品牌改为“梦鱼服 DreamingFish”，英文使用青色与紫色区分；右下角显示 `DreamingFish Updater` 与玩家端版本。
- 日志抽屉重做为“运行记录”，更新历史、运行记录、本地文件和本次更新统一使用同一套抽屉交互。

#### 启动与更新流程

- Minecraft 通过 Java Agent 启动玩家端；更新检查通过后立即发放启动许可，不等待界面关闭。
- Minecraft 开始启动后显示顶部提示，并在 15 秒后自动关闭；玩家主动打开详情抽屉时保持窗口打开。
- 取消首次启动修改安装位置的询问，始终使用实例绑定中的相对 `playerHome`，整合包移动到其它电脑后仍可工作。
- 支持玩家端程序签名自更新、活动版本切换和失败回退；程序更新检查不可用时继续进行整合包检查。
- 支持离线启动最近一次完整验证的安装；签名、哈希、路径或事务状态不可信时拒绝发放启动许可。
- 额外模组在普通同步下只提醒、不删除且不阻止启动；强制同步归档时明确显示服主已启用策略、文件数量和备份位置。

## 管理端

### [0.1.26] - 2026-08-14

- “外观与背景”新增“恢复默认配色”按钮，可一次恢复主强调色、次强调色、顶栏颜色、卡片颜色和 22% 顶栏底色不透明度；点击后先实时预览，只有保存个性化设置后才正式写入项目。
- 新增首页标题文字颜色设置，Web 实时预览、交互终端和参数式命令保持一致；“恢复默认配色”会同时恢复当前使用的米白色标题。

### [0.1.25] - 2026-08-14

- 玩家端个性化的“外观与背景”新增窗口顶栏底色不透明度设置，可使用滑块或百分比输入框实时预览；交互终端与参数式命令同步支持该配置。
- 旧项目没有透明度字段时自动使用原来的 22% 半透明效果，并校验配置必须处于 0% 到 100% 范围内。

### [0.1.24] - 2026-08-14

- 玩家端个性化页面拆分为“窗口品牌、外观与背景、玩家端音乐、玩家端页面”四个独立区域，并扩大编辑空间，长提示、按钮和音乐列表不再挤在同一块面板中。
- 背景图片、MP3 和页面配置 JSON 都可选择从当前浏览器电脑上传，或直接从管理端所在电脑 / 服务器导入；文件类型、大小和符号链接仍由管理端校验。
- “管理文件”的添加窗口支持在当前目标中创建新文件夹，空目录会立即出现在目录树中，加入文件后即可随整合包发布。
- 概览可以识别由另一管理端进程启动的新版下载服务，并通过仅本机可用的随机令牌控制通道安全停止或重启；无法确认身份的旧服务和其它占用端口的程序不会被误停。
- 修复普通页面切换为公告页再切回时未保存正文丢失的问题；普通正文与公告列表分别保存草稿，任意来回切换后仍可继续编辑。
- 移除从未在 Web 与交互终端公开的 `DEFAULT` 隐藏策略，新项目不再自动把 `options.txt`、`servers.dat` 当作特殊文件；旧数据库会在打开时迁移，旧签名发布仍能由玩家端兼容读取。

### [0.1.23] - 2026-08-13

- Web 管理端会主动检查公共下载服务：即使服务由另一个终端或管理端进程启动，也会识别为“正在运行”，不再误导服主重复启动并只显示端口占用报错。
- 公共下载服务状态每 10 秒自动刷新；如果端口被其他程序占用、服务已经卡住或健康检查异常，页面会给出对应的中文说明。
- 公共下载服务加入连接数量、请求时长和空闲连接限制，并改用有上限的工作线程，避免大量慢连接耗尽 Java 调度线程、导致健康检查和正常下载一起卡死。

### [0.1.22] - 2026-08-10

- 玩家端个性化页面改为可折叠的页面与新闻卡片，标题、页面类型、日期和操作按钮分层排列；长标题与窄编辑栏不再互相遮挡。
- 页面较窄时优先展示实时玩家端预览，页面配置工具栏改为独立操作行，避免超长编辑表单把预览和常用按钮埋在底部。

### [0.1.21] - 2026-08-10

- Web 侧边栏新增“外部托管”：可把整合包发布、玩家端自更新、个性化配置、签名和内容对象导出为保持现有 URL 结构的静态目录，适用于普通 HTTP、对象存储和 CDN。
- 静态目录支持安全增量导出，只补充新增或损坏对象；不可变对象优先准备，`latest`、`presentation` 等入口最后切换，并拒绝危险目录、符号链接和未完成导出。
- 支持管理端直接上传到 WebDAV / HTTP PUT，以及使用 AWS Signature V4 上传阿里云 OSS、AWS S3、Cloudflare R2 和 MinIO；密钥仅用于当次请求，不写入管理端、导出目录或玩家包。
- 上传默认并行处理不可变对象、跳过远端已有的相同内容并把可变入口放到最后；同时生成缓存规则示例和独立 `.sig` 文件，降低重复流量并避免半次上传被玩家看到。
- 内置公共下载服务同步提供 `.sig` 路由，便于先让旧地址完成玩家端升级，再平滑切换到固定更新域名后的外部托管。

### [0.1.20] - 2026-08-09

- 公共下载服务新增按项目签名的玩家端个性化配置接口，支持 ETag 条件请求；修改标题、配色、公告和自定义页面后无需扫描或发布整合包。
- Web 与交互终端补充生效范围说明：纯界面内容在玩家下次启动时自动获取，背景图片和音乐仍需正常创建发布。

### [0.1.19] - 2026-08-08

- 玩家端个性化页面新增多首 MP3 管理：可在 Web 中上传、命名、删除和清空音乐，上传失败或浏览器中断后临时文件会清理。
- 发布清单会校验音乐的文件名、SHA-256、大小和对象完整性；播放器更新后，音乐随项目发布清单增量同步。


### [0.1.18] - 2026-08-02

- 修复启用“管理端本机免登录”后点击登出会立即被本机免登录重新认证的问题；显式登出现在只让当前浏览器回到登录页，不会关闭全局本机免登录配置，重新登录或清除浏览器 Cookie 后可恢复免登录。
- Web 侧边栏新增“玩家端个性化”，集中配置标题栏品牌、首页标题与副标题、服务器地址、主题颜色、背景、新闻和自定义页面；创建项目时仍保留常用个性化选项，交互终端同步提供相同字段。
- 玩家端预览直接复用实际 Vue 玩家界面，品牌、颜色、新闻、自定义页面和背景变化可即时查看，避免演示界面与最终玩家端不一致。
- 背景图片支持从当前浏览器电脑直接选择、预览并上传到管理端；服务端校验图片格式和 32 MiB 大小限制，失败或中断后自动清理临时文件。
- Linux 管理端发行包改用固定版本 Temurin JDK 的 jmods 生成按需运行时，保留自带 Java 和完整功能，同时将运行时磁盘占用从约 134 MiB 降至约 59 MiB。
- 玩家端个性化改为统一的页面管理器：新增页面时可选择普通页面或公告页，页面与新闻均可删除、排序，旧新闻和旧自定义页面首次保存时自动迁移。
- Markdown 正文增加标题、加粗、列表、引用、链接和图片快捷排版按钮，并在按钮与编辑框切换焦点时保留文字选区。
- 页面配置支持易读 JSON 导入/导出；管理端可复制包含当前配置和字段约束的 AI 提示词，导入后先校验并确认，只在服主点击保存后写入项目。
- 交互终端同步支持添加、编辑、删除和排序普通页面/公告页，公告页可继续管理多条新闻。
- 管理端与玩家端落盘的 JSON 文件统一使用稳定的缩进和换行格式；HTTP API 与玩家边车逐行协议保持紧凑 JSON，不影响兼容性。

### [0.1.17] - 2026-08-02

- Web 管理端新增服务端账户注册、登录、登出和账户安全设置；可在注册时选择管理端本机是否免登录，公网请求始终需要有效会话。
- 管理密码使用随机盐和 600,000 次 PBKDF2-HMAC-SHA256 保存，账户文件采用原子替换并在 Linux 上限制为仅当前用户读写，不保存明文密码。
- 会话使用随机 `HttpOnly`、`SameSite=Strict` Cookie，限制空闲时间、最长时间和并发数量；写操作继续校验独立防伪令牌，登录失败按来源地址限速。
- 默认和旧配置升级后仍只监听 `127.0.0.1`；首次注册只能从本机或 SSH 隧道完成，未注册账户时不能启用 `0.0.0.0`。
- 公网远程登录及其后续 API 请求强制要求 HTTPS；仅信任来自本机反向代理的转发头，并防止伪造 `X-Forwarded-For` 把公网请求冒充成本机。
- 服务设置支持显式选择 Web 监听地址。同机 Caddy/Nginx 推荐继续代理 `127.0.0.1`，只有外部或隔离网络中的反向代理才需要开放 `0.0.0.0`。
- 注册、登录和账户安全界面适配白天/夜间模式；主题切换改为小图标，当前用户名与账户安全入口移至侧栏底部，登出使用独立红色图标。
- 修复强制同步目录表格的复选框列被错误拉宽、账户弹窗取消按钮受必填校验阻挡、设置页未提交 Web 监听地址，以及会话清理误刷新空闲时间的问题。

### [0.1.16] - 2026-07-31

- 将首次使用引导拆成“数据保存位置、玩家下载服务、Web 管理页面”三步，用“允许其他电脑连接 / 仅当前电脑连接”解释 `0.0.0.0` 与 `127.0.0.1`；首次创建时可明确选择 Web 或命令行，Web 端口被占用时会提示原因并回退到命令行创建，不再直接退出；管理端内明确区分本机访问与远程 SSH 隧道访问。
- Web 概览改为先引导创建整合包项目；创建表单补齐公网端口映射、管理目录和强制同步提示，创建成功后自动进入“管理文件”。
- “扫描与发布”更名为“管理文件”；整合包文件与单文件强制同步均支持按多级文件夹分类、文件夹递归勾选、半选状态、搜索后保持选择和当前列表全选。
- 在单文件强制同步前新增一级目录强制同步管理，明确目录同步会收敛额外文件并禁止豁免，而单文件强制同步只负责禁止玩家豁免选中的具体文件。
- 目录与单文件强制同步区域统一使用黄色警示界面并标注“谨慎启用”，与普通文件管理操作明确区分。
- 支持批量移除整合包文件，后端统一完成校验、归档、删除和扫描，执行中失败会恢复已经移出的文件。
- 将发布预览移动到单文件强制同步下方；扫描没有差异时明确显示“本次没有修改”。
- 修复回滚与创建项目弹窗中，必填字段为空时点击“取消”仍触发浏览器校验、无法关闭弹窗的问题。
- 所有 Web 操作错误统一改为页面中央弹窗，不再使用容易被遮罩挡住的右下角错误提示；路径选择改为 Web 内置的服务器文件浏览器，Windows 与 Linux 可使用同一界面，不再依赖容易藏到浏览器后面的系统选择窗口。
- 管理端 Web 新增白天/夜间模式切换，默认使用夜间模式并记住浏览器中的上次选择。
- 整合包文件、强制同步目录与单文件强制同步三个列表改为可折叠目录树：默认只显示顶层文件夹，展开状态分别记忆；删除文件后保持展开与滚动位置。
- “添加文件”改为先在目录树中选择目标文件夹（根目录或任意子目录），移除手动填写目标路径；从管理端所在服务器导入同样使用目录树选择。
- 上传改为临时分片加原子落盘，取消上传、关闭页面或传输失败都会清理临时分片，不会残留半成品文件；上传与文件浏览弹窗支持取消、右上角关闭和 Esc 三种方式关闭。
- 文件与文件夹使用不同图标（黄色文件夹、青蓝文档）并保留类型文字；移除各页面顶部整段说明，改为控件附近的简短提示，完整教程由独立文档维护。
- 创建项目弹窗中的“强制同步一级目录”同样使用黄色警示样式。
- 管理端发行包按平台只保留对应的 SQLite 原生库，Windows 使用最小 Java 模块集合，Linux 移除预生成 CDS 缓存，Windows/Linux 包约从 59/84 MiB 降至 41/60 MiB。
- SSH 隧道提示统一为完整的 `ssh -N -L 18080:127.0.0.1:18080 用户名@您的服务器地址` 示例。

### [0.1.15.1] - 2026-07-31

- Linux 发行包的 `dfs-admin` 启动脚本内置 `-Djava.net.preferIPv4Stack=true`，避免 Java 在部分 Linux 环境中只创建 IPv6 通配监听，导致 IPv4 NAT、端口映射或反向代理无法连接公共 HTTP 服务。
- 服主直接运行 `./dfs-admin` 即可获得 IPv4 监听，不再需要手动设置 `JAVA_TOOL_OPTIONS`；Windows 管理端行为不变。
- 发行构建会检查 Linux 启动脚本必须包含该参数，防止后续打包时回归。

### [0.1.15] - 2026-07-29

- 取消 Web 左侧独立“源文件”入口，把标准源文件搜索、上传、导入、移除和策略查看完整合并到“扫描与发布”页顶部，日常发布流程集中在一个页面完成。
- 限制组合页面中的源文件表格高度，保留独立滚动区域，避免长文件列表把预览、强制同步设置和发布表单推得过远。
- Web 启动的公共 HTTP 文件服务会在发布整合包或创建回滚发布后自动停止并重新绑定；页面明确提示重启成功。
- 如果版本已经不可变发布但 HTTP 服务重启失败，API 仍返回发布成功并携带独立警告，避免服主误以为发布失败后重复创建版本。
- Web 默认最低玩家端版本更新为 `0.1.14`，并新增运行中发布、自动重启、健康检查及最新清单切换的集成测试。

### [0.1.14] - 2026-07-29

#### Web 源文件管理

- 新增“源文件”页面，可搜索并查看标准源目录中的全部托管文件、大小、修改时间、发布状态，以及普通托管、目录强制和单文件强制策略。
- 支持从浏览器拖入或多选文件上传，并显示整批上传进度；多文件上传只在整批结束后扫描一次，避免每个文件重复扫描整合包。
- 支持调用管理服务器本机的 Windows 文件选择窗口导入文件；选择窗口使用置顶所有者，避免弹到浏览器或远程桌面后方。
- 覆盖或移除源文件前会把原文件校验后归档到 `data/source-archive/<项目>/<批次>/`，便于服主恢复误操作。
- 移除已发布文件时直接选择“从玩家端删除”或“放弃管理并保留”；强制同步目录内禁止放弃管理，规则与 CLI 完全一致。
- 修复单文件强制同步保存后仍显示“普通托管”的状态反馈；勾选时只更新当前行与计数，不重排列表。
- 修复尚未生成扫描预览时，Web 文件列表把实际存在的强制同步文件误报为“源文件缺失”的问题。

#### 首次部署与玩家端发布

- 新增“首次部署包”：管理端不需要完整 Minecraft 实例，即可从已发布对象重建签名玩家端、Bootstrap Agent、项目绑定、封面和指定历史版本的签名基线。
- 首次部署包是用于合并到服主本地实例的薄目录，不复制 `mods/`、`config/` 或 Minecraft 本体；管理端 Windows/Linux 发行包均内置所需 `support/bootstrap-agent.jar`。
- 修复薄首次部署包仍创建空强制同步目录的问题，部署包现在只保留更新器运行与签名校验所需内容。
- 发布玩家端程序时只需选择发行包解压根目录；管理端会读取 `active-player.properties`、语义版本目录或 jpackage 元数据，并自动定位唯一启动 EXE。

#### 项目、迁移与 Windows 交互

- Web、交互终端和参数式 CLI 均可修改项目显示名称，旧项目中已经保存的乱码名称可以直接修正。
- 管理端迁移到新目录后，如果 EXE 同级存在有效的 `data/management.db`，会自动优先使用并修正设置中的旧绝对路径；空 `data/` 不会触发误迁移。
- Windows CLI 使用原生控制台控制处理器拦截 `Ctrl+C` 和 `Ctrl+Break`，停止 HTTP 或 Web 服务后返回主菜单，不再连同整个管理端窗口一起退出。
- 玩家端没有改动，版本继续保持 `0.1.13`。

#### 验证

- 新增源文件上传、导入、覆盖归档、删除/放弃管理、强制目录约束、项目改名、玩家端目录自动识别、薄部署包、管理端搬家和 Windows 集成测试。
- 发行包冒烟测试新增管理端内置 Agent、玩家端根目录自动发布和无完整实例首次部署包的端到端校验。

### [0.1.13.1] - 2026-07-29

- 修复 Windows CMD/ConHost 使用代码页 936 时，管理端强制输出 UTF-8 导致整个中文菜单显示为 `鏁村悎鍖...` 的问题。
- 交互式 Windows 控制台现在读取实际代码页：CP936 使用 GBK 输出，CP65001 使用 UTF-8，其它代码页使用对应 Java Charset，并在无法识别时回退到系统原生编码。
- 输出被管道、脚本或文件重定向时仍固定为 UTF-8；Web 页面、JSON、数据库、清单和日志的 UTF-8 编码保持不变。
- 玩家端没有相关改动，版本继续保持 `0.1.13`。

### [0.1.13] - 2026-07-29

#### 发布与文件生命周期

- 删除标准源目录中的已发布文件后，管理端不再直接假定玩家端删除；发布前必须逐项选择“删除玩家文件”或“放弃管理并保留”，Web 页面支持逐项及批量设置。
- 放弃管理的路径会累计写入以后每个完整发布，旧整合包可直接跨多个版本更新到最新版而不会误删应保留文件；文件重新放回标准目录时自动恢复托管。
- 参数式发布新增 `--removed-files DELETE|RELEASE`，用于为尚未决定的移除项统一选择操作。
- 强制同步目录中的移除文件禁止放弃管理，避免与目录完整镜像规则冲突。

#### 强制同步与 Web 管理

- 新增单文件强制同步，可在 Web 页面搜索、勾选并保存具体托管文件；同目录其它文件仍保持普通同步。
- CLI 新增 `--force-sync-files` 与 `--clear-force-sync-files`，扫描会验证目标文件真实存在、未被排除且路径大小写一致。
- 项目源目录、玩家端 app-image、Minecraft 实例和封面路径旁新增“浏览”按钮，由管理端调用 Windows 原生文件或目录选择窗口。

#### Windows 启动与中文编码

- Windows 管理端改为原生控制台入口 `DreamingFishAdmin.exe`，不再依赖 `dfs-admin.cmd`，管理数据仍固定保存在 EXE 所在目录的 `data/`。
- Java 文件与重定向输出固定为 UTF-8；控制台输入优先严格解析 UTF-8，并为旧版 Windows Server 增加 GB18030 回退。
- 签名清单和内容对象的原子移动会短暂重试 Windows 杀毒或索引服务造成的瞬时文件锁，持续失败时仍安全中止发布。
- 已写入数据库的 `��` 或问号无法反向恢复，升级后需要重新填写相关更新记录。

### [0.1.9] - 2026-07-28

#### Web 管理界面

- 新增桌面优先的深色 Web 管理界面，覆盖项目切换与创建、项目和品牌设置、扫描预览、不可变发布、发布历史、回滚、玩家端程序发布、玩家实例制作及服务设置。
- 概览页可直接启动和停止面向玩家的公共 HTTP 文件服务；页面不轮询后台，空闲时没有持续数据库查询或网络请求。
- Web 后端使用 JDK 内置 `HttpServer` 和固定 4 个守护请求线程，不引入 Node、Electron 或常驻 Web 框架，适合资源有限的服务器。
- Web 管理端固定监听 `127.0.0.1:18080`，拒绝绑定公网地址；远程管理通过 SSH 本地端口转发访问。
- 所有写操作要求随机临时会话令牌，并加入同源资源策略、禁止嵌入、请求体大小限制及串行管理操作锁。
- 加密备份和恢复继续只允许从 CLI 执行，避免高风险运维动作暴露在浏览器入口。

#### 终端与配置

- 主菜单新增 `[10] 启动 Web 管理界面`；`[8]` 统一管理公共 HTTP 监听地址、公共端口和本机 Web 端口。
- 交互菜单中的 HTTP 文件服务和 Web 管理服务支持按 `Ctrl+C` 停止当前服务并返回主菜单，不再强制退出整个管理端。
- 管理设置升级到 schema 2；旧 schema 1 设置自动迁移并获得默认 Web 端口 `18080`，公共 HTTP 端口与 Web 端口禁止重复。
- 公共下载服务与 Web 管理服务使用独立监听器：默认分别为 `0.0.0.0:8080` 和 `127.0.0.1:18080`。
- 发行脚本新增 `-AdminOnly`，更新管理端时不再额外构建一个同版本号的玩家端包。

#### 验证

- 新增 Web 集成测试，覆盖静态资源与安全响应头、无会话令牌拒绝、项目创建、扫描、发布、历史读取及公共 HTTP 服务启停。
- 新增旧设置迁移测试，并在桌面视口完成创建项目、扫描、确认发布、发布历史和设置页的浏览器验证。

### [0.1.8] - 2026-07-27

#### 部署与交互

- 提供 Windows 与 Linux x64 自带 Java 运行时的命令行发行包，以及中文交互终端和首次启动向导。
- 管理数据固定保存在管理端根目录的 `data/`，不再要求服主配置内部数据目录。
- 标准整合包源目录只管理实际存在的内容，不强制创建 `mods/` 或 `config/`；服主可以只管理任意一个或多个所需目录。
- HTTP 默认监听 `0.0.0.0:8080`，项目公共地址可独立填写公网域名、IP 与映射端口。
- 支持项目创建、状态查看、设置修改、发布历史、玩家端程序发布、玩家实例制作、HTTP 设置和只读文件服务等交互菜单。

#### 发布能力

- 扫描源目录并预览新增、修改、删除、策略变化和模组元数据变化，确认后生成不可变签名发布。
- 发布记录支持中文、多行内容和 UTF-8 文本文件输入；交互模式使用 `@文件路径`，参数模式使用 `--changelog-file`，发布前回显实际保存内容。
- 增加只读发布历史接口，供玩家端展示所有历史版本及更新说明。
- 扫描 Forge、NeoForge 与 Fabric JAR，向清单写入稳定组件 ID 和显示名称。
- 可按一级目录独立启用强制同步；强制目录缺失时拒绝发布，显式空目录可发布“清空该目录”的目标状态。
- 发布玩家端完整 app-image 并签名，制作玩家实例时写入项目绑定、玩家端状态和指定历史版本的签名分发基线。
- 制作玩家实例时清理本机缓存、日志、事务、备份、模组启停和文件豁免偏好，避免把服主测试状态发给玩家。

#### 服务与运维

- 只读 HTTP 服务提供健康检查、签名发布、内容对象、玩家端程序及发布历史；支持 `Range`、`ETag` 和缓存头。
- 管理端以 SQLite 保存项目和发布元数据，以 SHA-256 内容寻址对象保存文件，相同内容只存一份。
- 提供密码保护的管理数据备份与恢复流程，保留项目私钥、身份和完整发布历史。
- 发行构建会校验管理端实际报告版本；冒烟测试支持玩家端 `0.1.9` 与管理端 `0.1.8` 分别指定版本。

## Bootstrap Agent

### [0.1.2] - 2026-07-27

- 保持 Java 8 兼容，在 Minecraft JVM 真正进入游戏前启动独立玩家端并等待许可。
- 通过仅监听回环地址的短生命周期通道与随机凭据收取许可，不向公网暴露控制端口。
- 启动活动玩家端和回退版本前都校验签名清单、项目身份与程序目录完整性，篡改时拒绝启动。
- 使用实例内相对玩家端路径，兼容 PCL 的版本隔离实例和整合包整体迁移。
- Agent 不在线自更新；要求更高 Agent 版本时必须重新制作并分发完整玩家实例。

## 共用协议与更新引擎

### [0.1.14 兼容更新] - 2026-08-14

- 玩家端个性化数据新增首页欢迎文字、顶栏颜色和布局卡片颜色；字段缺失时自动采用旧版默认值，已有项目与离线缓存无需手工迁移。
- 内容页面允许同时保留普通 Markdown 与公告文章列表，`announcementPage` 只决定玩家当前看到哪一种内容，不再把另一种编辑草稿视为无效数据。
- 新发布只生成普通托管与强制同步语义；历史清单中的 `DEFAULT` 仍按“仅缺失时安装”读取，保证旧玩家基线可以继续直接升级。

### [0.1.13 兼容更新] - 2026-07-29

- 发布清单新增 `releasedPaths`，以完整目标状态携带已放弃管理的历史路径；重新托管时会从该列表移除。
- 发布清单新增 `forcedSyncFiles`，只对精确规范路径启用强制管理，不扩散到父目录或相邻文件。
- 新增必要能力 `released-paths-v1` 与 `forced-file-sync-v1`；旧玩家端遇到不能理解的新语义时会明确拒绝发布，而不是静默执行错误策略。
- 清单验证新增排序、去重、路径冲突、强制文件存在性和文件策略检查，禁止同一路径同时处于托管、放弃管理或冲突强制状态。
- 更新规划器在跨版本更新时区分删除和放弃管理；普通删除继续尊重玩家本地豁免，单文件和目录强制同步继续覆盖本地豁免。

### [0.1.9 兼容更新] - 2026-07-27

- 发布清单增加可选模组组件 ID 与显示名称字段，并保持旧清单兼容。
- 新增发布历史协议和玩家端历史缓存模型。
- 新增玩家本地单文件、目录与组件豁免模型；规划器在普通同步中跳过豁免目标，在强制同步中明确覆盖本地规则。
- 更新结果记录完整的安装、删除与归档路径，为玩家端“本次更新”详情提供可靠数据。
- 保持按 SHA-256 对象增量下载：跨多个整合包版本直接比较本地已验证内容与最新完整清单，只下载本机缓存中缺失的对象。
- 任意旧正式整合包通过签名分发基线直接收敛到最新版本，已删除的旧官方文件不会被误判为玩家自选文件。
- 普通 `ENFORCED` 文件会修复缺失或损坏内容，但不会清理远程清单外的玩家额外文件；`DEFAULT` 文件仅在缺失时安装。
- 强制同步可按一级目录配置，远程不存在的额外文件先进入事务归档，整次更新成功后再提交到长期备份。
- 文件安装、删除、归档和状态提交使用持久化事务与原子替换；下载中断、进程崩溃或校验失败时可恢复或回滚。
- 强化 Windows 路径大小写、目录边界、符号链接、保留名称和实例逃逸校验，并在 Minecraft 正在运行时拒绝更新。
